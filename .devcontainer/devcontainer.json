// Dev container for the wps backend workspace.
// See https://aka.ms/devcontainer.json for format details.
{
  "name": "wps",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspaces/wps",
  // The osgeo/gdal image ships no SSH server, which the GitHub Codespaces CLI
  // needs in order to open a terminal.
  // The test suite uses testcontainers, which starts a throwaway PostgreSQL
  // container and therefore needs a Docker daemon inside the dev container.
  "features": {
    "ghcr.io/devcontainers/features/sshd:1": {},
    "ghcr.io/devcontainers/features/docker-in-docker:2": {}
  },
  // API (uvicorn) and web (vite) respectively.
  "forwardPorts": [8080, 3000],
  "postCreateCommand": "bash .devcontainer/post-create.sh",
  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "/workspaces/wps/backend/.venv/bin/python",
        "python.testing.pytestEnabled": true,
        "python.testing.cwd": "/workspaces/wps/backend",
        "terminal.integrated.defaultProfile.linux": "bash"
      },
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "charliermarsh.ruff",
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode"
      ]
    }
  }
}
